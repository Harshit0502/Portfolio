# Harshit Mahour — Streamlit Portfolio (Multipage)

Multipage, JSON-driven portfolio with RSS updates and a minimal Resume Q&A chatbot.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
